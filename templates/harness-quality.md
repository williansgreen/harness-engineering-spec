# Quality

## Actual Commands

```powershell
# replace with format/lint/static-analysis command
```

## Checks

- Build succeeds.
- Tests pass.
- Formatting or lint is clean.
- No unrelated file changes.
- No secrets or sensitive local config are included.
- Feature evidence is recorded through structured objects or `harness/update-evidence.ps1`.
- `progress.md` and `session-handoff.md` are concise enough for a fresh session to read quickly.
- Hardware, protocol replay, UI, deployment, and security acceptance docs are updated when those areas are touched.
- Harness docs match actual commands.
