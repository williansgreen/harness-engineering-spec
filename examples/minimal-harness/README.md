# Minimal Harness Example

This example shows the smallest practical harness for a documentation-only project.

It should pass:

```powershell
powershell -NoProfile -Version 5.1 -ExecutionPolicy Bypass -File ..\..\scripts\check-harness.ps1 -TargetPath .
```
