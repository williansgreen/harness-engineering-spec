# Build

## Actual Command

```powershell
dotnet build .\InstrumentControl.sln
```

## Notes

- This command becomes executable after `arch-001` creates the solution.
- Until then, the missing solution is an explicit blocker, not a completed feature.

