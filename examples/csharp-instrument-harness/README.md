# C# Instrument Harness Example

This example shows the harness files for a formal C# WinForms/WPF instrument-control project.

It is a harness example, not a full generated C# solution. Use it before creating the actual `.sln`.

