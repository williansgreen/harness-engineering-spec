# Review Prompt

Use `$csharp-winforms-wpf` to review `MainForm.cs` and `MainForm.Designer.cs` for WinForms Designer safety, MVP boundaries, device abstraction, and verification gaps.

Do not rewrite the whole application. Return findings first, then a concise fix direction.

